info:
  name: Update Profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/customer/profile"
  body:
    type: json
    data: |-
      {
        "name": "suraj paul",
        "organization": "honest co",
        "number": "8067575757",
        "industry": "Technology"
      }
  auth:
    type: bearer
    token: xxx

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
