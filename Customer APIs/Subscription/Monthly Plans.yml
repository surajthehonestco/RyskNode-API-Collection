info:
  name: Monthly Plans
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/customer/plans"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{customer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
