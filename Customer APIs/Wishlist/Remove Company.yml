info:
  name: Remove Company
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/api/customer/watchlists/1/companies/923"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{customer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
