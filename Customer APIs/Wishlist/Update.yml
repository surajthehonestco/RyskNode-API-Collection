info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/api/customer/watchlists/1"
  body:
    type: json
    data: |-
      {
        "name": "Gujarat Vendors Updated",
        "description": "Updated notes",
        "status": "evaluating"
      }
  auth:
    type: bearer
    token: "{{customer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
