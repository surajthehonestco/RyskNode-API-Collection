info:
  name: Company List
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/customer/watchlists/1/companies?search=reliance&per_page=10"
  params:
    - name: search
      value: reliance
      type: query
    - name: per_page
      value: "10"
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{customer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
