info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/api/customer/watchlists/1"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{customer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
