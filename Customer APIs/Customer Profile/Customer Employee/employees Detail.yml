info:
  name: employees Detail
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/customer/employees/1"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{customer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
