info:
  name: Billing List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/customer/billing-payments"
  params:
    - name: from
      value: 2026-01-01
      type: query
      disabled: true
    - name: to
      value: 2026-01-31
      type: query
      disabled: true
    - name: status
      value: paid
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{customer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
