info:
  name: Invoice
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/customer/billing-payments/{invoice}"
  auth:
    type: bearer
    token: "{{customer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
