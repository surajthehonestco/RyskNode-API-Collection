info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/customer/support-tickets"
  body:
    type: json
    data: |
      {
        "subject": "API Integration Issue",
        "category": "API",
        "description": "API authentication failed due to expired key.",
        "attachments": []
      }
  auth:
    type: bearer
    token: "{{customer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
