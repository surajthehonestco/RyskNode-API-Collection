info:
  name: close ticket
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/api/customer/support-tickets/5/close"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{customer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
