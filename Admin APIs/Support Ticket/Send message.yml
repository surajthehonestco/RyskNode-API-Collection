info:
  name: Send message
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/admin/support-tickets/5/messages"
  body:
    type: json
    data: |
      {
        "message": "Please check this issue.",
        "attachments": [
          "/path/to/screenshot.png"
        ]
      }
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
