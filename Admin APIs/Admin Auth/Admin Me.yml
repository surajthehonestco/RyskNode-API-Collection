info:
  name: Admin Me
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/admin/auth/me"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
