info:
  name: Detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/admin/sales/customers/11"
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
