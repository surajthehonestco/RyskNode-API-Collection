info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/admin/sales/customers?search=&status=&converted_from=&converted_to=&per_page=15"
  params:
    - name: search
      value: ""
      type: query
    - name: status
      value: ""
      type: query
    - name: converted_from
      value: ""
      type: query
    - name: converted_to
      value: ""
      type: query
    - name: per_page
      value: "15"
      type: query
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
