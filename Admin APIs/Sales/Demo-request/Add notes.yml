info:
  name: Add notes
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/admin/sales/leads/D8908KE/notes"
  body:
    type: json
    data: |-
      {
        "opportunity": "Client Follow Up - Jul 2026",
        "stage": "follow_up",
        "comment": "Client asked for another demo next week."
      }
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
