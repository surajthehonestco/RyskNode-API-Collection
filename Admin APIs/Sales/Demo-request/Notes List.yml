info:
  name: Notes List
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/admin/sales/leads/D8908KE/notes?per_page=10"
  params:
    - name: per_page
      value: "10"
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
