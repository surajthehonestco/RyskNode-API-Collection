info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/admin/sales/prospects?search=&stage=&created_from=&created_to=&per_page=15"
  params:
    - name: search
      value: ""
      type: query
    - name: stage
      value: ""
      type: query
    - name: created_from
      value: ""
      type: query
    - name: created_to
      value: ""
      type: query
    - name: per_page
      value: "15"
      type: query
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
