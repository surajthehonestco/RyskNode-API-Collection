info:
  name: Employee Detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/admin/employees/{{employee_id}}"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
