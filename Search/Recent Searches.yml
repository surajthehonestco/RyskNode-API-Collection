info:
  name: Recent Searches
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/customer/recent-searches"
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  variables:
    - name: token
      value: 98|bHAncTyU6XFOOFVvKZYPD1zTWvtpdCCbNlSTSiru3d4e12a1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Uses the same endpoint. Server must have `COMPANY_SEARCH_DRIVER=elasticsearch`, Elasticsearch env values configured, and `php artisan search:sync-companies --fresh` already run.
