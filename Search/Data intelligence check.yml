info:
  name: Data intelligence check
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/customer/company-data/1192928/access"
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{customer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
